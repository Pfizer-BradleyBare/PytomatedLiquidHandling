#############################################################
#
# Enabled: True: We can notify. False: We cannot notify
# Device ID: The identifies the source of the notification
# SMTP Server: The email server to route the notifications
# Sender Email: The sender address with access to the SMPT Server
# Always Notify List: This is a list of Emails or phone numbers that will always be notified.
#
#############################################################

Notify IDs:
  Email/Text Notification:
    Enabled: True
    Device ID: PPL2 MAM D709
    SMTP Server: SOMSMTP.AMER.PFIZER.COM
    Sender Email: DL-ARD_MAM_HAMILTON_USERS_STLOUIS_BIOPLACE@pfizer.com
    Always Notify Emails:
      - 354914b9.Pfizer.onmicrosoft.com@amer.teams.ms